# Example environment variables for Docker / local development
# Copy to .env and fill in secrets before running

# MongoDB connection used by Next.js server-side API routes
MONGODB_URI=mongodb://mongo:27017

# RPC endpoint for frontend to talk to contracts (use Besu or Hardhat URL)
NEXT_PUBLIC_RPC_URL=http://contracts:8545

# Example JWT secret used by API routes (if applicable)
JWT_SECRET=replace_with_a_secure_random_string
